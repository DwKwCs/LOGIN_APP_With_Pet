import 'package:flutter/material.dart';

class All3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text('건강'),
    ),
    );
  }
}